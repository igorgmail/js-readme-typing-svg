 
---

## Детальное описание

### 📦 `index.js`
**Назначение**: Точка входа приложения  
**Ответственность**:
- Инициализация Express сервера
- Настройка статических файлов
- Регистрация роутов
- Запуск сервера

---

### 🌐 `routes/`
**Назначение**: HTTP эндпоинты

#### `svg.js`
- **Эндпоинт**: `GET /svg`
- **Функция**: `handleSVG(req, res)`
- **Флоу**:
  1. Парсит query параметры → `parseQueryParams()`
  2. Применяет дефолты → `applyDefaults()`
  3. Генерирует SVG → `generateSVG()`
  4. Отправляет результат с Content-Type: `image/svg+xml`

---

### ⚙️ `config/`
**Назначение**: Конфигурация и обработка входных параметров

#### `params-parser.js`
- **Экспорт**: `parseQueryParams(query)`
- **Задача**: Конвертирует HTTP query параметры в типизированный объект
- **Обрабатывает**:
  - Преобразование типов (строки → числа, булевы)
  - Алиасы параметров (`duration` → `printSpeed`)
  - Парсинг `lines` (разделитель `;`)
  - Специальная логика для `center`

#### `defaults.js`
- **Экспорт**: 
  - `DEFAULT_PARAMS` - объект с дефолтными значениями
  - `applyDefaults(params)` - применяет дефолты к параметрам
- **Важно**: Синхронизирован с `client/js/defaults.js`

---

### 🎯 `core/`
**Назначение**: Ядро генерации SVG, основная бизнес-логика

#### `svg-generator.js`
- **Экспорт**: `generateSVG(params)`
- **Роль**: Главный координатор генерации
- **Флоу**:
  1. Парсит строки и применяет переменные → `parseLines()`
  2. Нормализует параметры
  3. Обрабатывает фон → `processBackground()`
  4. **⭐ Загружает и парсит шрифты → `getEmbeddedFontCSS()` → `{ css, parsedFont }`**
  5. Вычисляет стартовую позицию Y → `calculateStartY()`
  6. **⭐ Вычисляет анимацию с точными метриками → `calculateLinesAnimation(params, lines, startY, parsedFont)`**
  7. Рендерит итоговый SVG → `renderSVG()`

#### `animation-calculator.js`
- **Экспорт**: 
  - `calculateStartY(config)` - вычисляет вертикальное выравнивание
  - **⭐ `calculateLinesAnimation(params, lines, startY, parsedFont)` - вычисляет все параметры анимации**
- **Режимы работы**:
  - **Single line** - одна строка (печать → пауза → стирание)
  - **Replacing mode** - несколько строк сменяют друг друга
  - **Multiline mode** - строки печатаются построчно и остаются
- **⭐ Использует opentype.js метрики**: передаёт `parsedFont` во все вызовы `computeTextWidth()` и в конфигурации режимов стирания
- **Интегрирует**: эффекты стирания, позиционирование курсора, временные метки

#### `svg-renderer.js`
- **Экспорт**: `renderSVG(config)`
- **Задача**: Формирует финальный SVG-документ
- **Генерирует**:
  - `<defs>` с CSS шрифтов
  - Фоновый `<rect>`
  - Текстовые элементы с SMIL-анимацией
  - Элемент курсора с его анимацией
- **Функции**:
  - `generateDefs()` - секция стилей
  - `generateTextElement()` - текст с path-анимацией
  - `generateCursorElement()` - курсор (единый для всех строк)
  - `generatePerLineCursor()` - курсор для single/replacing режимов
  - `generateMultiLineCursor()` - курсор для multiline режима

---

### 🔄 `processors/`
**Назначение**: Обработка пользовательских данных

#### `variables-parser.js`
- **Экспорт**: 
  - `parseVariables(str)` - заменяет переменные в строке
  - `parseLines(lines)` - обрабатывает массив строк
- **Поддерживаемые переменные**:
  - `$DATE{...}` - форматирование даты через `Intl.DateTimeFormat`
  - `$RELATIVE_DATE{...}` - относительное время через `Intl.RelativeTimeFormat`
- **Формат**: `$VAR{param: value, param2: value2}`

---

### ✨ `effects/`
**Назначение**: Эффекты анимации (стратегии паттерн Strategy)

#### `erase/`
Режимы стирания текста:

**`EraseMode.js`** - базовый класс  
Методы:
- `calculateReplacingMode(config)` - для replacing режима
- `calculateMultiLineMode(config)` - для multiline режима  
- `calculateSingleLineMode(config)` - для одной строки

**`LineEraseMode.js`** - постепенное стирание посимвольно  
**`FadeEraseMode.js`** - стирание через fade (opacity)  
**`NoneEraseMode.js`** - без стирания

**`index.js`** - фабрика режимов:
- `getEraseMode(modeName)` - возвращает singleton instance
- `getAvailableEraseModes()` - список доступных режимов

#### `cursor/`
Конфигурация курсора:

**`index.js`**
- **Экспорт**:
  - `CURSOR_OPTIONS` - типы курсоров (none, straight, underlined, block, emoji, custom)
  - `getCursorInfo(cursorStyle)` - информация о курсоре
  - `isCursorAllowed(params)` - проверка совместимости с параметрами
  - `shouldHideCursorWhenFinished(params)` - нужно ли скрывать курсор в конце

#### `print/`
⚠️ **Статус**: В разработке / не используется  
Планируемые режимы печати (стандартный, expand и т.д.)

---

### 🔤 `fonts/`
**Назначение**: Работа со шрифтами

#### `data/`
JSON-файлы с метаданными шрифтов:
- `50-fontNames.json` - топ-50 шрифтов
- `allFontsData.json` - полная база шрифтов
- `webfonts.json` - веб-шрифты

#### `font-metrics.js` ⭐ NEW
- **Экспорт**:
  - `getCharWidth(char, fontSize, font, letterSpacing)` - точная ширина символа из opentype.js
  - `getApproximateCharWidth(fontSize)` - fallback без шрифта
  - `computeTextWidthPrecise(text, fontSize, font, letterSpacing)` - точная ширина текста
  - `getCharacterWidths(text, fontSize, font, letterSpacing)` - массив накопленных ширин
  - `getRemainingTextWidth(text, remainingChars, fontSize, font, letterSpacing)` - для анимации стирания
- **Задача**: Центральный модуль для точных расчётов метрик через opentype.js
- **Особенности**:
  - Использует `glyph.advanceWidth` из парсенного шрифта
  - Конвертирует font units в пиксели: `(advanceWidth / unitsPerEm) * fontSize`
  - Fallback на коэффициент 0.5 если шрифт не загружен
  - Поддержка emoji через регулярные выражения

#### `font-embed.js`
- **Экспорт**: `getEmbeddedFontCSS(config)` → `{ css, parsedFont }`
- **Задача**: Загружает шрифт, встраивает base64 в SVG и парсит через opentype.js
- **Флоу**:
  1. Определяет нужные символы из текста
  2. Формирует Google Fonts URL с `&text=`
  3. Загружает CSS
  4. Скачивает woff2 файл
  5. Конвертирует в base64 для встраивания
  6. **⭐ Парсит первый шрифт через `opentype.parse()`**
  7. Возвращает `{ css: string, parsedFont: Font | null }`

#### `generate-google-fonts-base64.js`
Утилита для предварительной генерации base64 шрифтов

---

### 🛠️ `utils/`
**Назначение**: Утилиты общего назначения

#### `text-utils.js`
Функции для работы с текстом и вычислений:
- `escapeXml(str)` - экранирование для SVG
- `validateAndSanitizeFontFamily(font)` - валидация шрифта
- `processBackground(bg)` - обработка цвета фона
- `parseLetterSpacing(value, fontSize)` - парсинг letter-spacing
- **⭐ `computeTextWidth(text, fontSize, letterSpacing, fontFamily, parsedFont)` - вычисление ширины текста через opentype.js метрики или fallback**
- **⭐ `computeTextX(text, fontSize, horizontalAlign, width, paddingX, letterSpacing, fontFamily, parsedFont)` - позиция X в зависимости от выравнивания**
- `formatLetterSpacingForSVG(value)` - форматирование для SVG-атрибута

---

## Поток данных
