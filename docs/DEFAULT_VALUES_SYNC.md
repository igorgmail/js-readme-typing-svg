# Синхронизация дефолтных значений

## Расположение файлов

Дефолтные значения параметров генератора SVG хранятся в двух местах:

1. **Сервер**: `server/utils/defaults.js`
2. **Клиент**: `client/js/defaults.js`

## ⚠️ ВАЖНО

При изменении дефолтных значений необходимо **синхронизировать оба файла**!

## Структура DEFAULT_PARAMS

```javascript
export const DEFAULT_PARAMS = {
  // Текст
  lines: 'Your text here',
  
  // Шрифт
  fontSize: 16,
  fontWeight: 800,
  fontFamily: 'monospace',
  letterSpacing: 10,
  lineHeight: 1.35,
  
  // Цвета
  color: '000000',
  background: 'transparent',
  
  // Размеры
  width: 800,
  height: 200,
  paddingX: 16,       // только на сервере
  paddingY: 20,       // только на сервере
  
  // Анимация
  printSpeed: 1, // символов в секунду
  eraseSpeed: 10, // символов в секунду
  delayBetweenLines: 800, // задержка между строками
  
  // Выравнивание
  horizontalAlign: 'left',
  verticalAlign: 'top',
  
  // Режимы
  multiLine: false,
  repeat: false,
  eraseMode: 'line'  // none | line | fade
};
```

## Различия между клиентом и сервером

### Только на сервере:
- `paddingX` - используется только в серверных вычислениях
- `paddingY` - используется только в серверных вычислениях
- `lineHeight` - используется только в серверных вычислениях

### На клиенте и сервере:
Все остальные параметры должны совпадать.

## Процесс синхронизации

1. Измените значения в `server/utils/defaults.js`
2. Скопируйте те же значения в `client/js/defaults.js`
3. Проверьте синхронизацию: `npm run check:defaults`
4. Убедитесь, что все значения совпадают

## Использование

### На сервере
```javascript
import { DEFAULT_PARAMS, applyDefaults } from './server/utils/defaults.js';

// Применение дефолтов к параметрам пользователя
const params = applyDefaults(userParams);
```

### На клиенте
```javascript
import { DEFAULT_PARAMS } from './client/js/defaults.js';

// Использование в классе GeneratorPage
this.defaults = DEFAULT_PARAMS;

// Сброс формы к дефолтным значениям
reset() {
  this.controls.lines.value = this.defaults.lines;
  this.controls['font-size'].value = this.defaults.fontSize;
  // ... и т.д.
}
```

## Кнопки в генераторе

### Кнопка "Сбросить" (Reset)
Кнопка "Сбросить" в генераторе:
1. Сбрасывает все поля формы к дефолтным значениям из `DEFAULT_PARAMS`
2. Корректно обрабатывает цветовые поля с jscolor (включая transparent)
3. Автоматически генерирует SVG с дефолтными параметрами
4. Обновляет превью

### Копирование URL
Для копирования сгенерированного URL используется метод `copyURL()`, который:
1. Проверяет, что URL был сгенерирован
2. Копирует его в буфер обмена
3. Показывает уведомление об успешном копировании

## Проверка синхронизации

Для проверки синхронизации используйте npm скрипт:

```bash
npm run check:defaults
```

Или напрямую через node:

```bash
node scripts/check-defaults-sync.js
```

Скрипт автоматически:
- ✅ Проверяет все общие параметры между клиентом и сервером
- ✅ Игнорирует параметры, которые есть только на сервере (paddingX, paddingY, lineHeight)
- ✅ Выводит детальный отчет о каждом параметре
- ✅ Завершается с кодом ошибки (exit 1) при обнаружении несоответствий
- ✅ Показывает параметры только на сервере для информации

### Когда запускать

- После изменения дефолтных значений в любом из файлов
- Перед коммитом изменений в defaults.js
- При ревью кода, связанного с дефолтными значениями
- Можно добавить в CI/CD pipeline для автоматической проверки

Пример успешного вывода:

```
🔍 Проверка синхронизации дефолтных значений
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ lines: "Your text here"
✅ color: "000000"
✅ background: "transparent"
...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ УСПЕХ: Все значения синхронизированы!
```

## История изменений

### 2025-12-02
- ✅ Изменена логика параметров `printSpeed` и `eraseSpeed`: теперь это количество символов в секунду (characters per second)
- ✅ Формула расчета: `duration = (line.length * 1000) / speed`
- ✅ Обновлены комментарии в defaults.js на клиенте и сервере
- ✅ Обновлена логика расчета в `animation-calculator.js` для всех режимов (single, replacing, multiline)
- ✅ Обновлены UI labels в generator.html: "Print/Erase Speed (characters per second)"
- ✅ Значения по умолчанию: printSpeed=1, eraseSpeed=10 символов в секунду
- ✅ Диапазон значений в UI: 0.1 - 100 символов в секунду для обоих параметров
- ✅ Курсор автоматически синхронизирован с новой логикой печати и стирания

### 2025-11-23
- ✅ Исправлена синхронизация значения `lines` между клиентом и сервером
- ✅ Добавлен импорт `DEFAULT_PARAMS` в `GeneratorPage`
- ✅ Исправлен метод `reset()` - теперь корректно сбрасывает форму к дефолтным значениям
- ✅ Добавлен метод `copyURL()` для копирования URL в буфер обмена
- ✅ Автоматическая генерация SVG после сброса формы
- ✅ Создан скрипт автоматической проверки синхронизации (`scripts/check-defaults-sync.js`)
- ✅ Добавлен npm-скрипт `check:defaults` в `package.json`
- ✅ Обновлены комментарии в файлах с дефолтами с указанием команды проверки

### 2025-11-21
- Создана синхронизация дефолтов между клиентом и сервером
- Добавлен файл `client/js/defaults.js`
- Обновлен метод `reset()` в `GeneratorPage`
- Добавлены предупреждающие комментарии в оба файла

