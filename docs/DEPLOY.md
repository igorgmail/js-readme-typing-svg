# 🚀 Деплой на различные платформы

## Vercel (Рекомендуется)

### Автоматический деплой

1. Установите Vercel CLI:
```bash
npm install -g vercel
```

2. Запустите деплой:
```bash
vercel
```

3. Следуйте инструкциям в терминале

### Через веб-интерфейс

1. Зайдите на [vercel.com](https://vercel.com)
2. Подключите ваш GitHub репозиторий
3. Vercel автоматически обнаружит настройки из `vercel.json`
4. Нажмите Deploy

После деплоя ваш SVG эндпоинт будет доступен:
```
https://your-project.vercel.app/svg?lines=Hello+World
```

---

## Heroku

1. Создайте `Procfile`:
```
web: node server.js
```

2. Инициализируйте git (если еще не сделано):
```bash
git init
git add .
git commit -m "Initial commit"
```

3. Создайте приложение на Heroku:
```bash
heroku create your-app-name
```

4. Задеплойте:
```bash
git push heroku main
```

---

## Railway

1. Зайдите на [railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Railway автоматически обнаружит Node.js и запустит `npm start`
5. Настройте домен в настройках проекта

---

## Render

1. Зайдите на [render.com](https://render.com)
2. Создайте новый Web Service
3. Подключите репозиторий
4. Настройте:
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
5. Нажмите Create Web Service

---

## DigitalOcean App Platform

1. Зайдите на [digitalocean.com](https://www.digitalocean.com/products/app-platform)
2. Создайте новое приложение
3. Подключите репозиторий
4. Выберите тип: Web Service
5. Настройте:
   - **Build Command**: `npm install`
   - **Run Command**: `npm start`
6. Выберите план и задеплойте

---

## Использование после деплоя

После успешного деплоя замените `http://localhost:3000` на ваш продакшн URL:

```markdown
![Typing SVG](https://your-app.vercel.app/svg?lines=Hello+World;Welcome!)
```

### В generator.html

Генератор автоматически будет использовать текущий домен, поэтому дополнительная настройка не требуется.

---

## Переменные окружения

Если нужно настроить порт через переменную окружения:

```bash
PORT=8080 npm start
```

Все платформы автоматически установят правильный `PORT`, поэтому дополнительная настройка обычно не требуется.

---

## Проверка деплоя

После деплоя проверьте работоспособность:

1. Откройте генератор:
```
https://your-app.vercel.app/generator.html
```

2. Протестируйте API:
```
https://your-app.vercel.app/svg?lines=Test
```

3. Используйте тестовую страницу:
```
https://your-app.vercel.app/test-server.html
```

