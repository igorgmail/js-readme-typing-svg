# 🔗 URL параметры

Полный список параметров для настройки SVG анимации.

## Базовая структура URL

```
https://js-readme-typing-svg.vercel.app/svg?param1=value1&param2=value2
```

## 📝 Текст и строки

### `lines` (обязательный)

Строки текста для анимации.

- **Тип:** `string`
- **Разделитель:** `;` (точка с запятой)
- **Пример:** `Hello;World;Welcome`

```
?lines=First+line;Second+line;Third+line
```

## 🎨 Внешний вид

### `fontSize`

Размер шрифта в пикселях.

- **Тип:** `number`
- **По умолчанию:** `16`
- **Диапазон:** `8` - `100`
- **Пример:** `?fontSize=24`

### `fontFamily`

Семейство шрифта из Google Fonts.

- **Тип:** `string`
- **По умолчанию:** `monospace`
- **Пример:** `?fontFamily=Roboto`

### `fontWeight`

Толщина шрифта.

- **Тип:** `number`
- **По умолчанию:** `400`
- **Значения:** `100`, `200`, `300`, `400`, `500`, `600`, `700`, `800`, `900`
- **Пример:** `?fontWeight=700`

### `color`

Цвет текста в HEX формате (без `#`).

- **Тип:** `string` (HEX)
- **По умолчанию:** `000000`
- **Пример:** `?color=00ff00`

### `background`

Цвет фона.

- **Тип:** `string` (HEX или `transparent`)
- **По умолчанию:** `transparent`
- **Пример:** `?background=1e1e1e`

### `letterSpacing`

Межбуквенный интервал.

- **Тип:** `string`
- **По умолчанию:** `normal`
- **Формат:** CSS значение (`0.1em`, `2px`, и т.д.)
- **Пример:** `?letterSpacing=0.15em`

## 📐 Размеры и отступы

### `width`

Ширина SVG в пикселях.

- **Тип:** `number`
- **По умолчанию:** `800`
- **Пример:** `?width=600`

### `height`

Высота SVG в пикселях.

- **Тип:** `number`
- **По умолчанию:** `200`
- **Пример:** `?height=150`

### `paddingX`

Горизонтальный отступ в пикселях.

- **Тип:** `number`
- **По умолчанию:** `16`
- **Пример:** `?paddingX=20`

### `paddingY`

Вертикальный отступ в пикселях.

- **Тип:** `number`
- **По умолчанию:** `20`
- **Пример:** `?paddingY=25`

### `lineHeight`

Межстрочный интервал (множитель).

- **Тип:** `number`
- **По умолчанию:** `1.35`
- **Диапазон:** `1.0` - `3.0`
- **Пример:** `?lineHeight=1.5`

## ⚡ Скорости анимации

Все скорости указываются в **символах в секунду**.

### `printSpeed`

Скорость печати текста.

- **Тип:** `number`
- **По умолчанию:** `10`
- **Диапазон:** `0.1` - `100`
- **Пример:** `?printSpeed=15`

### `eraseSpeed`

Скорость стирания текста.

- **Тип:** `number`
- **По умолчанию:** `10`
- **Диапазон:** `0.1` - `100`
- **Пример:** `?eraseSpeed=20`

### `delayBetweenLines`

Задержка между строками в миллисекундах.

- **Тип:** `number`
- **По умолчанию:** `800`
- **Диапазон:** `0` - `5000`
- **Пример:** `?delayBetweenLines=1000`

## 🎯 Выравнивание

### `horizontalAlign`

Горизонтальное выравнивание текста.

- **Тип:** `string`
- **По умолчанию:** `left`
- **Значения:** `left`, `center`, `right`
- **Пример:** `?horizontalAlign=center`

### `verticalAlign`

Вертикальное выравнивание текста.

- **Тип:** `string`
- **По умолчанию:** `top`
- **Значения:** `top`, `middle`, `bottom`
- **Пример:** `?verticalAlign=middle`

### `center`

Алиас для центрирования по обеим осям.

- **Тип:** `boolean`
- **По умолчанию:** `false`
- **Действие:** Устанавливает `horizontalAlign=center` и `verticalAlign=middle`
- **Пример:** `?center=true`

## 🎭 Режимы анимации

### `typingMode`

Режим печати текста.

- **Тип:** `string`
- **По умолчанию:** `expand`
- **Значения:**
  - `expand` — SVG расширяется при печати
  - `static` — фиксированный размер SVG
- **Пример:** `?typingMode=static`

### `eraseMode`

Режим стирания текста.

- **Тип:** `string`
- **По умолчанию:** `line`
- **Значения:**
  - `line` — постепенное стирание посимвольно
  - `fade` — затухание через opacity
  - `none` — без стирания
- **Пример:** `?eraseMode=fade`

### `cursorStyle`

Стиль курсора.

- **Тип:** `string`
- **По умолчанию:** `straight`
- **Значения:**
  - `none` — без курсора
  - `straight` — вертикальная линия `|`
  - `underlined` — подчеркивание `_`
  - `block` — блок █
  - `emoji` — эмодзи (нужно указать в `cursorEmoji`)
  - `custom` — свой символ (нужно указать в `cursorCustom`)
- **Пример:** `?cursorStyle=block`

### `cursorEmoji`

Эмодзи для курсора (если `cursorStyle=emoji`).

- **Тип:** `string`
- **По умолчанию:** `✨`
- **Пример:** `?cursorStyle=emoji&cursorEmoji=🚀`

### `cursorCustom`

Свой символ для курсора (если `cursorStyle=custom`).

- **Тип:** `string`
- **Пример:** `?cursorStyle=custom&cursorCustom=>`

## 🔄 Поведение

### `multiLine`

Многострочный режим (строки остаются на экране).

- **Тип:** `boolean`
- **По умолчанию:** `false`
- **Действие:**
  - `false` — строки сменяют друг друга
  - `true` — строки печатаются друг под другом
- **Пример:** `?multiLine=true`

### `repeat`

Повторять анимацию в цикле.

- **Тип:** `boolean`
- **По умолчанию:** `true`
- **Пример:** `?repeat=false`

## 📋 Примеры комбинаций

### Простая анимация

```
?lines=Hello+World&fontSize=24&color=00ff00
```

### Многострочный текст с настройками

```
?lines=Line+1;Line+2;Line+3
&multiLine=true
&center=true
&fontSize=20
&color=ffffff
&background=1e1e1e
```

### С переменными

```
?lines=Today:+$DATE{dateStyle:+medium};Time:+$DATE{timeStyle:+short}
&multiLine=true
&fontSize=18
```

### Кастомизация анимации

```
?lines=Fast+typing;Slow+erase
&printSpeed=20
&eraseSpeed=5
&delayBetweenLines=1500
&eraseMode=fade
&cursorStyle=block
```

### Полная настройка

```
?lines=Welcome;I'm+a+Developer;Check+my+projects
&fontSize=28
&fontFamily=Roboto
&fontWeight=700
&color=00ff00
&background=1e1e1e
&width=900
&height=180
&center=true
&multiLine=true
&printSpeed=15
&eraseSpeed=20
&delayBetweenLines=1000
&typingMode=static
&eraseMode=fade
&cursorStyle=block
&repeat=true
```

## 🎯 Советы по использованию

### URL кодирование

Пробелы и специальные символы нужно кодировать:
- Пробел → `+` или `%20`
- `;` → `%3B` (если не разделитель строк)
- `{` → `%7B`
- `}` → `%7D`

### Оптимальные настройки

**Для GitHub профиля:**
```
?fontSize=24-32
&center=true
&width=600-800
&height=80-120
```

**Для README проекта:**
```
?fontSize=20-24
&multiLine=true
&width=800-1000
&typingMode=static
```

**Для статистики:**
```
?fontSize=16-18
&color=666666
&repeat=false
```

### Производительность

- Используйте `typingMode=static` для фиксированного размера
- Ограничьте количество строк (3-5 оптимально)
- Избегайте очень большие SVG (`width` > 1200px)

---

💡 **Рекомендация:** Используйте [визуальный генератор](https://js-readme-typing-svg.vercel.app/generator) для создания правильных URL без ручного кодирования.

