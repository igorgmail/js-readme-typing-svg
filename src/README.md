# Source Modules

Эта папка содержит основные JavaScript модули проекта.

## Структура модулей

### 📦 Core Modules

#### `typing-svg.js`
Основной модуль, содержащий функцию `createMultilineBlockTypingSVG()`.

**Экспорт:**
- `createMultilineBlockTypingSVG(options)` — создание SVG с анимацией

**Импорты:**
- `animations.js` — функции анимаций
- `variables.js` — парсинг переменных

---

#### `animations.js`
Модуль с функциями анимаций печати и стирания текста.

**Экспорт:**
- `typeLine(lineIndex, lines, printSpeed, updateCaret)` — печать строки
- `eraseLine(lineIndex, lines, eraseSpeed, updateCaret, textEls, multiLine)` — стирание строки
- `eraseBlock(lines, eraseSpeed, updateCaret, textEls)` — блочное стирание
- `eraseWipeDown(NS, svg, width, height, eraseSpeed, textEls)` — эффект шторки
- `eraseFade(eraseSpeed, textEls)` — эффект затухания

---

#### `variables.js`
Модуль для парсинга и обработки динамических переменных в тексте.

**Экспорт:**
- `parseVariables(str)` — парсит переменные в одной строке
- `parseLines(lines)` — парсит массив строк с переменными

**Поддерживаемые переменные:**
- `$DATE{...}` — форматирование текущей даты (Intl.DateTimeFormat)
- `$RELDATE{...}` — форматирование относительного времени (Intl.RelativeTimeFormat)

---

#### `url-parser.js`
Модуль для парсинга URL параметров и конвертации их в опции SVG.

**Экспорт:**
- `parseURLParams()` — парсит query параметры из URL
- `urlParamsToOptions(urlParams)` — конвертирует URL параметры в опции
- `initFromURL(createSVGFunction, container)` — инициализация из URL
- `optionsToURL(options, baseURL)` — генерация URL из опций

---

## Использование

### Базовый импорт

```javascript
import { createMultilineBlockTypingSVG } from './src/typing-svg.js';

createMultilineBlockTypingSVG({
  lines: ["Hello", "World"],
  fontSize: 32
});
```

### Импорт отдельных модулей

```javascript
// Только анимации
import { typeLine, eraseFade } from './src/animations.js';

// Только парсинг переменных
import { parseVariables } from './src/variables.js';

// Только URL парсер
import { parseURLParams, urlParamsToOptions } from './src/url-parser.js';
```

## Зависимости между модулями

```
typing-svg.js
    ↓
    ├─→ animations.js (функции анимаций)
    └─→ variables.js (парсинг $DATE, $RELDATE)

url-parser.js (независимый модуль)
```

## Архитектура

- **ES6 Modules** — нативные модули без бандлера
- **Нет зависимостей** — только нативные браузерные API
- **Intl API** — DateTimeFormat, RelativeTimeFormat для интернационализации
- **SVG** — динамическая генерация через `document.createElementNS`

## Добавление новых модулей

1. Создайте файл в `src/`
2. Экспортируйте функции через `export`
3. Импортируйте в нужные модули
4. Обновите этот README

## Тестирование

Для тестирования модулей используйте:
- `../test.html` — тестовая страница с отладкой
- `../index.html` — основное демо
- `../generator.html` — генератор URL

## Браузерная поддержка

Требуется поддержка:
- ES6 Modules (import/export)
- Intl.DateTimeFormat
- Intl.RelativeTimeFormat
- SVG DOM API

Поддерживается в Chrome 76+, Firefox 79+, Safari 14+, Edge 79+

